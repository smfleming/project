# use a function without loading the package
# package::function
usethis::use_git_config(
  user.name = "Steve Fleming",
  user.email = "stephen.fleming@ucl.ac.uk",
  init.defaultBranch = "main") # <-- not necessary but kinder than 'master'
